# Workspace

This directory contains runtime artifacts created by the agent system.

- `projects/` — per-project working directories
- `bundles/` — exported workflow bundles
- `logs/` — session and execution logs
